# trebor entzminger , date 8/18/2021, calculate mean median and mode 

varible <- c(1, 2, 3, 4, 5)

mean <- mean (variable1)


median <- median(variable1)

variable2 <- c(1, 2, 3, 4, 5, 2)

install.packages("modeest")
